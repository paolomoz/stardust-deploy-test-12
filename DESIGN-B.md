# DESIGN-B — Amplify Instrument Serif

Inherits shared Mode A (DESIGN.md). Delta = the captured display face, used
once today, promoted into the page's structural voice.

- **Section headlines**: Instrument Serif at editorial scale — hero ~96px,
  section openers ~56–64px. The serif carries the argument.
- **Body / UI**: Akkurat retreats to body copy, captions, buttons; Fragment
  Mono keeps the telemetry labels. Three-face hierarchy made legible:
  serif = statement, grotesque = explanation, mono = machine.
- **Rhythm**: more editorial — wider measure on lead paragraphs, serif drop
  openers, generous leading under each serif head.
- Keeps A's improvements (hero hierarchy, density, trusted-by, narration,
  security badges) — B is A plus the type bet.

Motion: static. (Differentiates from C by axis: typography/composition, not
motion.)
