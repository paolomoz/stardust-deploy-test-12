# DESIGN-C — Live-data motion (cinematic, live-systems)

Identical IA to A; the bet is motion. Register: live-systems. Mercury's
numbers come alive — hero metric ticker, count-up stats on reveal
([data-countup]), capacity/yield bars filling ([data-fill]), live pulse
dots on realtime rows, periodic refresh sweep across live tiles, tile
cascade entrances ([data-tile-anim]). Scroll-scrub hero extended into a
data scroll narrative. Full prefers-reduced-motion fallback forces every
animated element to its final state. Palette + type pinned.
