---
_provenance:
  writtenBy: stardust:uplift
  writtenAt: 2026-06-19T04:10:09.299Z
  againstInput: https://www.adaline.ai/
  readArtifacts:
    - stardust/current/_brand-extraction.json
    - stardust/current/pages/home.json
---

# "What if…" candidates — https://www.adaline.ai/

Walked all eight catalog candidates against the captured brand surface.

### 1 · Display-typography amplification — **QUALIFIED → assigned to B**
**What if Instrument Serif grew from one footer line into the page's
structural voice?**
- Evidence: Instrument Serif appears exactly once (footer, 104px); every
  other heading is Akkurat grotesque. The most expressive face is reserved
  for a single closing beat. `headings[3]`, `_brand-extraction.json § type`.
- Axis: composition / typography (static). Best serves **B**.

### 2 · Photography re-foregrounding — qualified (not selected)
**What if the landscape photography breathed at editorial scale?**
- Evidence: sublime nature photography (mountain valley, snow ridge) sits
  *behind* product screenshots at reduced scale. `screenshot`, `media`.
- Note: borderline — the imagery is curated-sublime, tied to the "never stop
  learning / growth" metaphor, so not disqualified as generic stock; but it
  is not proprietary brand photography either. Ranked below candidate 1 for B.

### 3 · Live-data promotion — **QUALIFIED → assigned to C**
**What if the live trace / behavior / eval telemetry was the hero — a system
you watch learning in real time?**
- Evidence: the strongest captured asset is operational telemetry — trace rows
  (`06:10:12 · agent.plan.revise · 3.8s · $0.10`), a behavior taxonomy
  (B01..B25 with ISSUE/CHANGED/NEW), eval generation — but it all lives below
  the hero and is rendered as static stills. The product literally *self-improves
  in real time*; the page never shows motion. `landmarks[3,4,5]`, `ctas`.
- Axis: live-data + motion. Naturally amplified by register `live-systems`.
  Best serves **C**.

### 4 · Signature-gesture extension — disqualified
No single defining gesture (monogram / illustrated motif) beyond the wordmark;
the "live panel" is the closest, but that is already captured by candidate 3.

### 5 · Voice-register pivot — weak (not selected)
Mild tonal contrast (organic "never stop learning" vs. clinical trace logs),
but neither tone is confined to the margins, so there is no clearly underused
register to foreground.

### 6 · Color-ladder re-weighting — qualified (not selected)
Deep forest `#203B14` and dark `#2A332A` appear only on pill CTAs and the
footer band (< 10% of surface). Could be promoted to a primary ground. Valid,
but a weaker bet than 1 and 3; folded into B and C as supporting moves rather
than a standalone variant.

### 7 · Audience-routing reframe — disqualified
Single clear audience (AI/ML teams shipping agents). No multi-intent
compression to untangle.

### 8 · Motif vocabulary swap — qualified (not selected)
The indexed behavior list (B01..B25) is an under-used alternate to the
card-panel primitive. Interesting, but subordinate to candidate 3, which
already foregrounds that exact surface. Folded into C.

---

## Resolution

- **C** takes **Live-data promotion** (register `live-systems` naturally
  amplifies it through motion).
- **B** takes **Display-typography amplification** (composition axis — keeps
  B and C differentiated by axis, not just intensity).
- Two strong qualified candidates remain after disqualification (1 and 3),
  plus three supporting qualifications (2, 6, 8) — the brand surface is rich
  enough for three differentiated variants. No `--two-variants` fallback.
